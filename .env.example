# ===================================================================
# LLM Bootcamp Project - Environment Configuration Template
# ===================================================================
# This file serves as a template for your local .env file.
# Copy this file to .env and fill in your actual API keys.
#
# IMPORTANT SECURITY NOTES:
# - Never commit your actual .env file to version control
# - The .env file is already in .gitignore
# - Rotate your API keys regularly
# - Use read-only or scoped keys when possible
# - For production deployments, use Docker secrets or a secrets manager
# ===================================================================

# -------------------------------------------------------------------
# Environment Configuration
# -------------------------------------------------------------------
# Set to 'production' in production environments
# Set to 'development' for local development
ENVIRONMENT=development

# -------------------------------------------------------------------
# OpenAI API Key (REQUIRED)
# -------------------------------------------------------------------
# Used by: All chatbot features (Basic Chat, Agent Chat, RAG, MCP Agent)
# Get your key from: https://platform.openai.com/api-keys
# Format: sk-proj-...
OPENAI_API_KEY=your-openai-api-key-here

# -------------------------------------------------------------------
# Tavily API Key (OPTIONAL)
# -------------------------------------------------------------------
# Used by: Search-Enabled Chat (Chatbot Agent)
# Get your key from: https://tavily.com/
# Format: tvly-...
# Leave empty if not using search-enabled chat
TAVILY_API_KEY=your-tavily-api-key-here

# -------------------------------------------------------------------
# MCP Server URL (OPTIONAL)
# -------------------------------------------------------------------
# Used by: MCP Agent feature
# For local development: http://localhost:8000
# For Docker Compose with MCP profile: http://mcp-server:8000
# Leave empty if not using MCP Agent
MCP_SERVER_URL=http://localhost:8000

# ===================================================================
# Setup Instructions
# ===================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and replace placeholder values with your actual API keys
# 3. Verify .env is in .gitignore (it should be by default)
# 4. Run the application: streamlit run Home.py
#
# For Docker deployments:
#   docker-compose up --build
#
# For production with Docker secrets:
#   See docker-compose.yml for Docker secrets configuration
# ===================================================================
